import { api } from '../../services/Api';
// import { LOGIN, LOGINPART } from '../../utils/constants/endPoints';
import { LOGOUT } from '../../utils/constants/action_types';

export const login = (username, password, navigation = null) => dispatch => { };
